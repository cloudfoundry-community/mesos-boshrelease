/var/vcap/sys/log/mesos-agent/*.log {
  missingok
  rotate <%= p('mesos.agent.logrotate.rotate') %>
  compress
  delaycompress
  notifempty
  copytruncate
  size <%= p('mesos.agent.logrotate.size') %>
  su <%= p('mesos.agent.user') %> <%= p('mesos.agent.group') %>
}

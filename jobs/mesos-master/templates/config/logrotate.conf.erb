/var/vcap/sys/log/mesos-master/*.log {
  missingok
  rotate <%= p('mesos.master.logrotate.rotate') %>
  compress
  delaycompress
  notifempty
  copytruncate
  size <%= p('mesos.master.logrotate.size') %>
  su <%= p('mesos.master.user') %> <%= p('mesos.master.group') %>
}
